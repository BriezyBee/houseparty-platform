version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: houseparty
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: houseparty_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
